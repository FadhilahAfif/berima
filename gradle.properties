# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. For more details, visit
# https://developer.android.com/r/tools/gradle-multi-project-decoupled-projects
# org.gradle.parallel=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# AGP 9 ships built-in Kotlin support. KSP registers its generated Kotlin
# source sets via `kotlin.sourceSets`, which AGP 9 disallows by default.
# This flag re-enables the old behavior so KSP can plug in. Remove once
# KSP + AGP 9 negotiate this natively.
android.disallowKotlinSourceSets=false

# Do not auto-scan the machine for JVM installations. VSCode's Red Hat Java
# extension ships a JRE (no jlink), and Gradle daemon provisioning picks it
# up and fails. With auto-detect off, Gradle uses the JVM that launched it
# (i.e. whatever JAVA_HOME points to). Set JAVA_HOME to a full JDK 21 —
# Android Studio JBR at "C:\Program Files\Android\Android Studio\jbr" works.
org.gradle.java.installations.auto-detect=false